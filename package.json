{
  "name": "fizgo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.14.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
